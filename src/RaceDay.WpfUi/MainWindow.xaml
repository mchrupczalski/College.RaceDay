<Window x:Class="RaceDay.WpfUi.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ctrl="clr-namespace:RaceDay.WpfUi.Controls"
        xmlns:vm="clr-namespace:RaceDay.WpfUi.ViewModels"
        mc:Ignorable="d"
        Title="Race Day"
        Height="720"
        Width="850"
        d:DataContext="{d:DesignInstance vm:MainViewModel, IsDesignTimeCreatable=True}" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <ctrl:AppHeader Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" />
        <ContentControl Grid.Row="1" Grid.Column="1" Content="{Binding ActiveViewModel}" />
    </Grid>
</Window>